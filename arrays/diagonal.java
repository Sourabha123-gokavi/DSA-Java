public class diagonal {
    public static int sum_diagonal(int arr[][]){
        int sum=0;
        for(int i=0;i<arr.length-1;i++){
            // primary diagonal
            sum=sum+arr[i][i];
            // secondary diagonal sum
            if(i!=arr.length-1-j)
            
        }
        
        
        
        return sum;
    }
}
